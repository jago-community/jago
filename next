- add map file to replace reading `~/local/jago` with `~/cache/git@...`
- ensure instrument.vim contains all basic vim configuration from ~/.vimrc
- try git operation without password first, then request and try again if it fails
- store ssh key password if needed with https://docs.rs/ripasso/0.5.1/ripasso/
